# EXCEPTIONS - galdr tool only
# Max 4 unsafe_files, max 4 boundary_files
# Paths MUST contain "/" (no bare basenames)
#
# Empty quarantine = clean start.
# Let violations reveal architectural debt.

[quarantine]
unsafe_files = []
boundary_files = []
