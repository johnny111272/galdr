# Schema-to-module mappings for generate_structures.py
# Galdr only consumes the final anthropic render schema produced by regin.

[checkpoint]
agent-anthropic-render = "anthropic_render"
agent-output-structure = "output_structure"
agent-output-content = "output_content"
agent-output-display = "output_display"
